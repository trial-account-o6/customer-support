services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: supportflow-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: supportflow-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: always
